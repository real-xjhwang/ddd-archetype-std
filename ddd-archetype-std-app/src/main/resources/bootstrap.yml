spring:
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        server-addr: ${spring.cloud.nacos.server-addr}
        import-check:
          enabled: true
        file-extension: yml
      discovery:
        server-addr: ${spring.cloud.nacos.server-addr}
        namespace: xxx
